version: '2'

syncs:
  pan_src:
    src: .
    volumes:
      - .:/app
    sync_strategy: native_osx
    sync_args: ["-prefer newer", "-copyonconflict"]
    sync_excludes: &pan_src_excludes
      - ".git"
      - "log"
    watch_excludes: *pan_src_excludes
